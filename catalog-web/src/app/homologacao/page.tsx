export const metadata={title:"Homologação | Briland",robots:{index:false,follow:false}};
export default async function Homologacao({searchParams}:{searchParams:Promise<{erro?:string;retorno?:string}>}) { const query=await searchParams; return <main className="gate"><form action="/api/homologacao" method="post" className="gate-card"><div className="logo-mark">BRILAND</div><p>Catálogo web em homologação</p><h1>Área de validação</h1><p>Informe a senha fornecida para visualizar a nova experiência.</p><input type="hidden" name="retorno" value={query.retorno||"/"}/><input name="senha" type="password" placeholder="Senha de homologação" required autoFocus/>{query.erro&&<span className="form-error">Senha incorreta.</span>}<button type="submit">Acessar catálogo</button></form></main>; }
